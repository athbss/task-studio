// Default organization ID - can be changed later when multi-org support is added
export const DEFAULT_ORG_ID = 'taskmaster';

// Organization display name
export const DEFAULT_ORG_NAME = 'Taskmaster';
